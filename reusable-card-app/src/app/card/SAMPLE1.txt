 @Test
    void testCashAllocationModelList_Line297_IgnoreT_True() {
        List<String> lines = new ArrayList<>();
        String inputLine = " ".repeat(59) + "T";
        lines.add(inputLine);

        // Mock the getter method
        Mockito.when(cashAllocationDomain.getIgnoreLineNotContainT()).thenReturn(Constant.IGNORE_LINE_NOT_CONTAIN_T);

        // Debugging: Print byte values
        String extractedSubstring = inputLine.toUpperCase(Locale.FRENCH).substring(59, 60);
        System.out.println("Extracted Substring: [" + extractedSubstring + "]");
        System.out.println("Extracted Substring Bytes: " + java.util.Arrays.toString(extractedSubstring.getBytes(StandardCharsets.UTF_8)));
        System.out.println("IGNORE_LINE_NOT_CONTAIN_T: [" + cashAllocationDomain.getIgnoreLineNotContainT() + "]");
        System.out.println("IGNORE_LINE_NOT_CONTAIN_T Bytes: " + java.util.Arrays.toString(cashAllocationDomain.getIgnoreLineNotContainT().getBytes(StandardCharsets.UTF_8)));

        List<CashAllocationModel> result = cashAllocationDomain.cashAllocationModelList(lines, "DATA_REC_FILE");

        assertNotNull(result);
        assertEquals(0, result.size());
    }