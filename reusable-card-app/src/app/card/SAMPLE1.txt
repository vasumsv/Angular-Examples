  @Test
    void testUpdateCashAllocationStatus_RS_Flow() {
        // Create test data
        ProcessCashRequest processCashRequest = new ProcessCashRequest();
        processCashRequest.setInvoiceRefs(Arrays.asList("RS123", "RS456"));
        processCashRequest.setPaymentIds(Collections.singletonList("PAY123"));

        // Mock invoiceServiceApi.getInvoice
        InvoicesList invoicesList = new InvoicesList();
        when(invoiceServiceApi.getInvoice("RS123,RS456")).thenReturn(invoicesList);

        // Execute the method
        cashAllocationDomain.updateCashAllocationStatus("matched", processCashRequest, "TEST_USER");

        // Verify that invoiceServiceApi.getInvoice is called
        verify(invoiceServiceApi, times(1)).getInvoice("RS123,RS456");
    }