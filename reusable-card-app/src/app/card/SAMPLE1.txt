  @Test
    public void testPaymentDetailsByValueDate_DirtyTest_273_to_282() {
        // Arrange
        String valueDateStart = "2023-10-27";
        String valueDateEnd = "2023-10-28";

        // Mock repository to return a stream of CashAllocationModel
        List<CashAllocationModel> mockModels = new ArrayList<>();
        CashAllocationModel model1 = new CashAllocationModel();
        CashDetails details1 = new CashDetails();
        details1.setId(1L);
        model1.setCashDetails(details1);
        model1.setId(1L);

        CashAllocationModel model2 = new CashAllocationModel();
        CashDetails details2 = new CashDetails();
        details2.setId(2L);
        model2.setCashDetails(details2);
        model2.setId(2L);

        mockModels.add(model1);
        mockModels.add(model2);

        when(cashAllocationRepositoryApi.findByValueDateLesserThanEqual(any(LocalDate.class)))
                .thenReturn(mockModels.stream()); // Return a stream

        // Act
        cashAllocationDomain.paymentDetailsByValueDate(valueDateStart, valueDateEnd);

        // No assertions, just aiming for line coverage
    }