    @Test
    void testUpdatePaymentLines_Success() {
        // Setup test data
        String user = "testUser";
        List<UpdatePaymentLines> finalDataForInsertion = new ArrayList<>();

        // Create a sample UpdatePaymentLines object and add it to the list
        UpdatePaymentLines samplePaymentLine = new UpdatePaymentLines();
        // Set necessary properties of samplePaymentLine based on your UpdatePaymentLines class
        // For example:
        samplePaymentLine.setPaymentReference("REF123"); // Replace with actual property names
        samplePaymentLine.setPaymentDate("2023-10-27");
        samplePaymentLine.setPaymentAmount(100.0);
        // ... set other required properties

        finalDataForInsertion.add(samplePaymentLine);

        // Mock invoiceServiceApi behavior
        when(invoiceServiceApi.sendPaymentDetailsToInvoiceService(anyList())).thenReturn(null);

        // Execute the method
        String result = cashAllocationDomain.updatePaymentLines(user, finalDataForInsertion);

        // Verify the result
        assertEquals("SUCCESSFUL", result);

        // Verify interactions
        verify(invoiceServiceApi, times(1)).sendPaymentDetailsToInvoiceService(anyList());
    }