  @Test
    public void testUploadPaymentAbFileViaReferential_fileDeleteSuccess() throws IOException, NoSuchFieldException, IllegalAccessException {
        AbDomain abDomain = new AbDomain();

        // Create a mock ReferentialServiceApi using Mockito
        ReferentialServiceApi mockReferentialServiceApi = Mockito.mock(ReferentialServiceApi.class);

        // Use reflection to set the referentialServiceApi field
        Field field = AbDomain.class.getDeclaredField("referentialServiceApi");
        field.setAccessible(true);
        field.set(abDomain, mockReferentialServiceApi);

        // Create a temporary file
        File tempFile = File.createTempFile("test", ".txt");
        tempFile.deleteOnExit();

        // Write some content to the file
        Files.write(tempFile.toPath(), "Test content".getBytes());

        // Call the method
        abDomain.uploadPaymentAbFileViaReferential(tempFile.getAbsolutePath());

        // You can optionally verify that the mock was called
        Mockito.verify(mockReferentialServiceApi, times(1))
                .uploadFileToGaiaServer(any(ByteArrayResource.class), anyString(), anyString());
    }