   @Test
    void testCreatePetJson_ValidationStatus_Q() throws Exception {
        // Arrange
        com.socgen.cash.allocation.entity.PetDetails petDetails = new com.socgen.cash.allocation.entity.PetDetails();
        petDetails.setValidationStatus("Q");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", com.socgen.cash.allocation.entity.PetDetails.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, petDetails);

        // Assert
        assertNotNull(result);
    }