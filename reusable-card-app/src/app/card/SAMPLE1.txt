   @Test
    void testSavePaymentDetails() {
        // Create test data
        String fileName = "testFile.txt";
        File file = new File(fileName);
        List<String> fileLines = new ArrayList<>();
        fileLines.add("line1");
        fileLines.add("line2");
        List<CashAllocationModel> cashAllocationModels = new ArrayList<>();
        cashAllocationModels.add(new CashAllocationModel());

        // Mock dependencies
        when(fileProcessingService.getFile(fileName)).thenReturn(file);
        when(fileProcessingService.readFile(file)).thenReturn(fileLines);
        when(cashAllocationDomain.cashAllocationModelList(fileLines, fileName)).thenReturn(cashAllocationModels);

        // Call the method
        cashAllocationDomain.savePaymentDetails(fileName);

        // Verify interactions
        verify(cashAllocationRepositoryApi, times(1)).savePaymentDetails(cashAllocationModels);
    }