    @Test
    public void testTransferFile_FileNotFoundException() throws Exception {
        LocalDate date = LocalDate.now();
        List<CashAllocationModel> cashAllocationModelList = Collections.emptyList();

        doThrow(new FileNotFoundException("File not found")).when(abDomain).uploadPaymentAbFileViaReferential(anyString());

        String result = abDomain.transferFile(date, cashAllocationModelList);

        verify(logger).error(anyString(), any(FileNotFoundException.class));
        assertNull(result); // Assuming transferFile returns null on exception
    }

    @Test
    public void testTransferFile_IOException() throws Exception {
        LocalDate date = LocalDate.now();
        List<CashAllocationModel> cashAllocationModelList = Collections.emptyList();

        doThrow(new IOException("Exception while writing file")).when(abDomain).uploadPaymentAbFileViaReferential(anyString());

        String result = abDomain.transferFile(date, cashAllocationModelList);

        verify(logger).error(anyString(), any(IOException.class));
        assertNull(result); // Assuming transferFile returns null on exception
    }