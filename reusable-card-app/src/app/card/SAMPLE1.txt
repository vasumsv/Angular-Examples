// Assert
assertNotNull(aggregateAmount);
assertEquals(1, aggregateAmount.size());
GroupingKey expectedKey = new GroupingKey("entity1", "USD");
assertEquals(BigDecimal.valueOf(100), aggregateAmount.get(expectedKey));

assertNotNull(aggregateInvoiceCount);
assertEquals(1, aggregateInvoiceCount.size());
assertEquals(1, aggregateInvoiceCount.get(expectedKey)); // Assuming one invoice

assertNotNull(aggregateInvoiceReferences);
assertEquals(1, aggregateInvoiceReferences.size());
assertEquals("ABC123DEF,", aggregateInvoiceReferences.get(expectedKey));