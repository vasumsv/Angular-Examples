 @Test
    void testCreatePetJson_ValidationStatus_Q() throws Exception {
        // Arrange
        SrgPaymentRequest pmtDetails = new SrgPaymentRequest();
        pmtDetails.setValidationStatus("Q");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", SrgPaymentRequest.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, pmtDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_ValidationStatus_W() throws Exception {
        // Arrange
        SrgPaymentRequest pmtDetails = new SrgPaymentRequest();
        pmtDetails.setValidationStatus("W");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", SrgPaymentRequest.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, pmtDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_AccountingIndicator_N() throws Exception {
        // Arrange
        SrgPaymentRequest pmtDetails = new SrgPaymentRequest();
        pmtDetails.setAccountingIndicator("N");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", SrgPaymentRequest.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, pmtDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_AccountingIndicator_T() throws Exception {
        // Arrange
        SrgPaymentRequest pmtDetails = new SrgPaymentRequest();
        pmtDetails.setAccountingIndicator("T");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", SrgPaymentRequest.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, pmtDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_DealType_CA_And_0() throws Exception {
        // Arrange
        SrgPaymentRequest pmtDetails = new SrgPaymentRequest();
        pmtDetails.setDealType("CA");
        pmtDetails.setGetDealType("0");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", SrgPaymentRequest.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, pmtDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_DealType_NotCA_Or_Not0() throws Exception {
        // Arrange
        SrgPaymentRequest pmtDetails = new SrgPaymentRequest();
        pmtDetails.setDealType("CB"); // or any other value except CA
        pmtDetails.setGetDealType("1"); // or any other value except 0

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", SrgPaymentRequest.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, pmtDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }