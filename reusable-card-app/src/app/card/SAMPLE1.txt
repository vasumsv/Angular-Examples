  @Test
    void testCreatePetJson_ValidationStatus_Q() throws Exception {
        // Arrange
        PetDetails petDetails = new PetDetails();
        petDetails.setValidationStatus("Q");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", PetDetails.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, petDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_ValidationStatus_W() throws Exception {
        // Arrange
        PetDetails petDetails = new PetDetails();
        petDetails.setValidationStatus("W");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", PetDetails.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, petDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_AccountingIndicator_N() throws Exception {
        // Arrange
        PetDetails petDetails = new PetDetails();
        petDetails.setAccountingIndicator("N");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", PetDetails.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, petDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_AccountingIndicator_T() throws Exception {
        // Arrange
        PetDetails petDetails = new PetDetails();
        petDetails.setAccountingIndicator("T");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", PetDetails.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, petDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_DealType_CA_And_0() throws Exception {
        // Arrange
        PetDetails petDetails = new PetDetails();
        petDetails.setDealType("CA");
        petDetails.setGetDealType("0");

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", PetDetails.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, petDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }

    @Test
    void testCreatePetJson_DealType_NotCA_Or_Not0() throws Exception {
        // Arrange
        PetDetails petDetails = new PetDetails();
        petDetails.setDealType("CB"); // or any other value except CA
        petDetails.setGetDealType("1"); // or any other value except 0

        // Act
        Method method = SrgPmtApiClient.class.getDeclaredMethod("createPetJson", PetDetails.class);
        method.setAccessible(true);
        Object result = method.invoke(srgPmtApiClient, petDetails);

        // Assert
        assertNotNull(result); // Check if the method returns a result
    }